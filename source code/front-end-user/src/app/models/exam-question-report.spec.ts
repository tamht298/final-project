import { ExamQuestionReport } from './exam-question-report';

describe('ExamQuestionReport', () => {
  it('should create an instance', () => {
    expect(new ExamQuestionReport()).toBeTruthy();
  });
});
